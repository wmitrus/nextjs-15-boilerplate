import { ReactNode } from "react"

export default function {{pascalCase name}}Layout({
    children, 
  }: {
    children: ReactNode
  }) {
    return (
      <section>
        {children}
      </section>
    )
  }